version: 2
updates:
  # Keep the SHA-pinned GitHub Actions in .github/workflows up to date.
  # Dependabot bumps both the pinned SHA and its trailing version comment.
  - package-ecosystem: github-actions
    directory: "/"
    schedule:
      interval: weekly
